{%- comment -%}
PYLO Collapsible Content Section (FAQ/Accordion)
Now uses only global color scheme variables for all colors. All static color settings removed. Styling is fully dynamic and merchant-editable via theme settings. See theme.liquid for color scheme variables.
Modular CSS for this section is loaded below.
{%- endcomment -%}

{{ 'section-pylo-collapsible-content.css' | asset_url | stylesheet_tag }}

<section class="pylo-collapsible-content">
  <h2 class="pylo-collapsible-content__heading">{{ section.settings.heading }}</h2>
  <div class="pylo-collapsible-content__items">
    {%- for block in section.blocks -%}
      <div class="pylo-collapsible-content__item">
        <button class="pylo-collapsible-content__title" type="button" aria-expanded="false" aria-controls="faq-{{ block.id }}">
          {{ block.settings.title }}
        </button>
        <div class="pylo-collapsible-content__body" id="faq-{{ block.id }}">
          {{ block.settings.content }}
        </div>
      </div>
    {%- endfor -%}
  </div>
</section>

{% schema %}
{
  "name": "PYLO Collapsible Content",
  "settings": [
    { "type": "text", "id": "heading", "label": "Heading", "default": "Frequently Asked Questions" }
  ],
  "blocks": [
    {
      "type": "item",
      "name": "Item",
      "settings": [
        { "type": "text", "id": "title", "label": "Title" },
        { "type": "textarea", "id": "content", "label": "Content" }
      ]
    }
  ],
  "max_blocks": 8,
  "presets": [
    { "name": "Collapsible Content", "blocks": [ { "type": "item" }, { "type": "item" } ] }
  ]
}
{% endschema %} 